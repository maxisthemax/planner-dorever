import { createMuiTheme } from "@material-ui/core/styles";

const theme = createMuiTheme({
  palette: {
    primary: { main: "#2189ff", contrastText: "#fff" },
    secondary: { main: "#f59c00", contrastText: "#fff" }
  }
});

export default theme;
